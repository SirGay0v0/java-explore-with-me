DROP TABLE IF EXISTS phantom CASCADE;

CREATE TABLE IF NOT EXISTS phantom
(
    id        BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    app       VARCHAR(255)                         NOT NULL
);